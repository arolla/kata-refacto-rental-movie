{
  "name": "kata-refacto-rental-movie",
  "version": "1.0.0",
  "description": "Typescript implementation of the kata \"Rental Move\"",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "tslint -c tslint.json 'src/**/*.{ts,tsx}'",
    "pretest": "npm run lint"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^23.1.1",
    "intern": "^4.2.0",
    "jest": "^23.1.0",
    "remap-istanbul": "^0.11.1",
    "ts-jest": "^22.4.6",
    "tslint": "^5.10.0"
  },
  "jest": {
    "collectCoverage": true,
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ],
    "transform": {
      "\\.(ts|tsx)$": "<rootDir>/node_modules/ts-jest/preprocessor.js"
    },
    "testRegex": "(/test/.*|(\\.|/)(test|spec))\\.(js?|ts?)$"
  },
  "dependencies": {
    "typescript": "^2.9.2"
  }
}
